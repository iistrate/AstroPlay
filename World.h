#ifndef WORLD_H
#define WORLD_H

#include "Tile.h"
#include <vector>

class World {
private:
public:
	World() {}
	~World(){}

	//std::vector < std::vector < Tile > > mapTiles (ySize, std::vector < Tile >(xSize));
};

#endif // World.h